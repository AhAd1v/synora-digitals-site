# For local dev + the generic-ASGI-host and cPanel deployment paths (see
# README.md). Vercel deploys use the separate, trimmed ../requirements.txt
# instead (Vercel requires it at the project root) — keep both in sync.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1
slowapi==0.1.9
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
a2wsgi==1.10.7
